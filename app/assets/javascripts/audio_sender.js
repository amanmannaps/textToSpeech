/*var inputFile1 = window.parent.document.getElementById("audio1-upload");
var sendButton1 = window.parent.document.getElementById("send1-button");


// Define a function to simulate a user selecting a file
function selectFile(filePath) {
  inputFile1.value = "";
  inputFile1.dispatchEvent(new MouseEvent("click", { bubbles: true }));
//  inputFile.files = [new File([filePath], "audio.mp3")];
  inputFile1.files = [new File([filePath], "test1.mp3")];
}

// Define a function to simulate a user clicking the send button
function clickSendButton() {
  sendButton1.dispatchEvent(new MouseEvent("click", { bubbles: true }));
}

// Call the functions to select a file and click the send button

// selectFile("/path/to/audio.mp3");
selectFile("/home/ajin/Desktop/PlanetSpark/Task 2/ChatTest/test1.mp3");
clickSendButton();
*/